export interface Product {
    name: string,
    year: number,
    price: number,
    storage: number,
    color: string,
    category: string,
    imageUrl: string,
    descriptionUrl: string,
    _id: string
}